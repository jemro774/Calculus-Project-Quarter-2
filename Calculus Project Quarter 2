#Jack Robey and Aidan Marshall
#11/8/18
#Calculus Project Quarter 2

function = input('Enter a function: ')
interval1 = input('Enter the lowest value of the closed interval: ')
interval2 = input('Enter the highest value of the closed interval: ')

eval(function)

'''for i in range(interval1,interval2+1):
    





"""terms = int(input("Enter the function's number of terms: "))

for i in range(1,terms+1):
    coefficient = 

interval1 = float(input('Enter the start value of the interval: '))
interval2 = float(input('Enter the end value of the interval: ')) """
